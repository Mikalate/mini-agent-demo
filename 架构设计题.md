## 模块一：Context / Performance

一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？

**回答：**

压缩只动最早、已闭合的完整用户回合——assistant 的 tool_calls、reasoning_content 和对应的 tool 消息绝不拆开，当前正在跑的 run 也不压，否则协议对不上，模型会开始幻觉工具结果。被压掉的回合交给 LLM 生成滚动摘要，按"已确认事实/用户偏好/工具结果/未解决事项"四个板块结构化；摘要与旧消息的压缩标记放同一事务提交，失败就回退用旧摘要，原始消息只打标记不删除，留一条追溯后路。摘要放在 system 段并标注为低优先级历史，最近十几条完整消息原样保留，再配合 prompt caching 把固定前缀缓存住。

保证流畅的核心是三件事：压缩粒度要"整"（工具链不拆，当前 run 不压），摘要要有结构（四板块，模型一眼能找回"聊过什么、还剩什么没做"），失败必须可回退（摘要生成失败不阻塞本轮）。另外压缩是异步做的，别让 LLM 摘要请求卡在用户输入路径上；每次压缩把压缩前后 token、摘要版本记进 trace，压缩本身也要有 SLO。



## 模块二：Memory

和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？

**回答：**

写入侧：对话结束后异步跑一个抽取 LLM，把对话蒸馏成事实/偏好/经验条目，存进向量+关键词混合存储；经验类记忆不能让模型自己写，要由 runtime 在 run 结束后根据实际结果蒸馏，否则会积累幻觉记忆。

召回侧：用户提问先做 embedding 语义检索命中相关记忆（半个月前的话题自然命中），再用触发式召回补充——本次 run 的错误码、工具序列命中历史教训就带出来；命中结果注入 system 低优先级段，每条带元数据（时间、来源 session、置信度），并明确标注"历史记忆，以最新对话为准"。

还要处理时效和冲突：置信度按时间衰减，命中就回升；新旧记忆冲突时以新为准并更新旧条目；记忆段和 system 段分开算 token 预算，超了先丢低分条目；top-K 控制在个位数，避免噪音反过来污染主任务。核心就一句：按需召回、防幻觉写入、低优先级注入、时效衰减。

业界主流方案目前有几条路线：

- Mem0 是工程上用最多的记忆层，统一 add/search/get_all 接口，向量与图混合存储，写入时自动检测语义重复并更新；
- 阿里与武汉大学 2026 年初提出的 AgeMem 把长短记忆统一成一套框架，用 ADD、UPDATE、DELETE、RETRIEVE 四类操作原语驱动记忆的读写与更新；
- UCSD 的 Mem-α 走强化学习路线，让模型自己学会何时记忆、记什么、如何更新；
- MemMA 用多智能体协调记忆的构建、检索与利用全循环。



## 模块三：Task

1.对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？

**回答：**

忘目标本质是状态不在模型手里，而在我们手里。方案上我按三件套组合用：目标锚定、显式计划持久化、Checkpoint。

- 目标锚定是每轮 system 里固定放 goal/constraints/progress 三段，只由 runtime 更新 progress，成本最低但只治标；
- 显式计划持久化才是治本——任务拆成步骤写进数据库（Claude Code 落成 `.tasks/` 的 DAG，blockedBy 表达依赖），模型每次决策前查 todo 状态而不是靠记忆，某步失败超过阈值就触发 replan，只重写剩余步骤；
- Checkpoint负责兜底：每完成一步把中间状态和产物落盘，上下文重置甚至进程重启都能续。

再往上可以加反思机制，把失败蒸馏成教训存进记忆，下次同类场景直接带出，代价是额外 LLM 调用，要加频率上限；子代理分解是另一种思路，主代理只规划汇总、把上下文压力摊给子代理（Claude Code 最多 49 个并行），换来的是编排复杂度。这些方案不互斥，实际项目里组合用：提醒 + 状态落盘 + 可恢复，缺一不可——单靠提醒救不了状态丢失，单靠计划救不了计划过时。



2.用户给 Agent 下达任务：每天早上 9 点根据昨天聊天情况做复盘总结。你会怎么设计？

**回答：**

分四层：调度、取数、复盘执行、投递。调度用 cron，每天 9 点整即表达式 `0 9 * * *`；9 点按用户时区算，每次触发生成 job_id 做幂等防重，防 cron 双触发和服务重启重放，服务宕机错过就补跑或跳过告警。取数按 created_at 把昨天 0 点到 24 点的消息、todo 变更、run 状态捞出来——注意必须带上滚动摘要，被压缩的旧消息只有摘要还在，否则"昨天聊了什么"是残缺的。

复盘执行用独立 session/独立 run 和主聊天隔离，输出 schema 固定（昨日回顾/未完成/问题与风险/今日建议），LLM 失败重试，再失败降级成规则模板粗摘要，复盘绝不能静默丢失。投递走 webhook（飞书/企微/邮件），失败重试退避，用户不在线就进消息队列。最后把复盘结果 upsert 回长期记忆，下次用户问"最近咋样"能直接引用。



## 模块四：Tool / Session Runtime

Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？

**回答：**

核心原则：提交即返回、结果必须回来、但不等。工具可以声明异步，或运行时超时自动转后台（比如超过 5 秒自动后台化，Claude Code 的 BashTool 就是这么干的，还有 15 秒助理模式自动后台）。提交后立刻拿到 task_id，agent loop 不阻塞；每个任务在 registry 里有完整状态机 pending→running→succeeded/failed/cancelled，带 TTL、并发上限、输出缓冲截断（防止大结果撑爆上下文），绑定 user/session 防串。

完成通知走双通道：拉模式是每次 LLM 调用前 drain 一个完成队列，把已结束任务的结果包装成标准 tool_result 回填，让模型基于真实结果做下一步决策——这是主通道，因为结果要喂回模型；推模式是 webhook/SSE 通知用户端"任务完成了，结果在哪"，用户也可以主动查（/tasks）。失败也要回填错误信息，模型据此修正或重试；任务要持久化，进程重启不丢。注意异步完成时原调用早已返回，回填要绑定 task_id 而不是原 call id（Claude Code 的做法是专用 task 工具让模型主动查/读）。



## 模块五：Agent Runtime 架构对比

Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

**回答：**

协议层：

- Anthropic 走 content blocks，assistant 消息里 text/thinking/tool_use 可以交错，工具结果以 tool_result block 用 user 角色回填，stop_reason=tool_use；
- OpenAI 兼容格式是 assistant.message.tool_calls 数组 + role:tool 消息按 tool_call_id 回填，没有原生 thinking，各家自己加方言（DeepSeek 的 reasoning_content 要原样回传）。

产品层：

Claude Code 多出来的是终端 TUI 渲染、ask/auto/bypass 权限体系、MCP 生态、子代理、后台任务、JSON 流式输出，这些是它作为 Runtime 的护城河。

优缺点一句话：

Claude Code 用复杂度和厂商锁定换体验与可控性，适合交互式终端场景；OpenAI 兼容格式用薄协议换生态和简单，工具定义用 JSON Schema、SDK 成熟，但执行、权限、UI、恢复的工程责任全在开发者（本项目 mini_agent 就是从零搭的这套）。

并且两边正在趋同：GLM 提供 Anthropic 兼容端点可接入 Claude Code，Anthropic 也提供 OpenAI 兼容端点。

